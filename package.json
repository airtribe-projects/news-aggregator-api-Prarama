{
  "name": "news-aggregator-api",
  "version": "1.0.0",
  "description": "Assignment 2 for backend engineering launchpad",
  "main": "src/server.js",
  "scripts": {
    "pretest": "node -e \"if (parseInt(process.version.slice(1).split('.')[0], 10) < 18) { console.warn('⚠️ Warning: Project is recommended to run on Node.js v18+. Current: ' + process.version); }\"",
    "test": "tap test/*.js --disable-coverage",
    "start": "node src/server.js",
    "dev": "nodemon src/server.js"
  },
  "author": "Airtribe",
  "license": "ISC",
  "devDependencies": {
    "nodemon": "^3.1.14",
    "supertest": "^6.3.4",
    "tap": "^21.7.4"
  },
  "dependencies": {
    "axios": "^1.18.1",
    "bcrypt": "^6.0.0",
    "body-parser": "^2.3.0",
    "dotenv": "^17.4.2",
    "express": "^4.22.2",
    "jsonschema": "^1.5.0",
    "jsonwebtoken": "^9.0.3",
    "mongoose": "^9.7.4"
  },
  "engines": {
    "node": ">= 18.0.0"
  }
}
